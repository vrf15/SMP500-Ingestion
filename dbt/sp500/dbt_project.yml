name: 'sp500'
version: '1.0.0'
 
profile: 'sp500'
 
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
 
clean-targets:
  - "target"
  - "dbt_packages"
 
models:
  sp500:
    staging:
      +schema: stg_smp500
      +materialized: view
    core:
      +schema: core_smp500
      +materialized: table
    marts:
      +schema: marts_smp500
      +materialized: table